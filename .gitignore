# Dependencies
node_modules

# Build output
dist
.output
.nuxt
.nitro
.cache
*.tsbuildinfo

# Generated TypeDoc API reference (npm run docs:api)
/api-docs

# Test / coverage
coverage
.vitest
test-results
playwright-report

# Logs
*.log
npm-debug.log*

# Editor / OS
.DS_Store
*.local
.eslintcache

# Env
.env
.env.*
!.env.example

# Storybook
storybook-static

# IDE
.idea
/package-lock.json

# Release automation (local working copies, cloned from external repos)
.release-repo
.docs-release-repo
.dev-repo

# Internal task specs and phase reports — not meant to be pushed
/task
/reports

# Session working log — local resume notes, not meant to be pushed
/RESUME.md

# Self-hosted docs-app fonts, copied from node_modules by `npm install`
# (postinstall) — regenerable, not hand-authored assets.
docs-app/src/assets/fonts
